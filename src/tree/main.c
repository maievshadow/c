#include "tree.h"
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    PTreeNode root = initTreeNode(10);
    insertTreeNode(root);
    //createTreeNode(root, 10);
    //createTreeNode(root, 20);
}